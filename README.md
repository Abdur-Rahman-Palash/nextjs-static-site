j# আয়মান টিচিং হোম এন্ড আইটি ফার্ম - Educational Website

আয়মান টিচিং হোম এন্ড আইটি ফার্মের জন্য তৈরি একটি আধুনিক, রেসপন্সিভ শিক্ষামূলক ওয়েবসাইট। Next.js 15, Tailwind CSS, এবং DaisyUI দিয়ে তৈরি এই স্ট্যাটিক সাইট কোর্স, সার্ভিস এবং তথ্য প্রদর্শন করে।

## ✨ বৈশিষ্ট্যসমূহ

- **আধুনিক ডিজাইন**: DaisyUI কম্পোনেন্ট ব্যবহার করে পরিষ্কার, পেশাদার ডিজাইন
- **রেসপন্সিভ**: মোবাইল-ফার্স্ট ডিজাইন যা সকল ডিভাইসে কাজ করে
- **স্ট্যাটিক জেনারেশন**: দ্রুত লোডিং এবং SEO-এর জন্য অপটিমাইজড
- **শিক্ষামূলক ফোকাস**: কোর্স, সার্ভিস এবং যোগাযোগের জন্য ডেডিকেটেড পেজ
- **PWA রেডি**: প্রগ্রেসিভ ওয়েব অ্যাপ বৈশিষ্ট্য
- **SEO অপটিমাইজড**: মেটা ট্যাগ, সাইটম্যাপ এবং রোবটস.txt

## 📁 প্রজেক্ট স্ট্রাকচার

```
nextjs-static-site/
├── src/
│   ├── app/                    # Next.js App Router pages
│   │   ├── about/             # About page
│   │   ├── contact/           # Contact page
│   │   ├── courses/           # Courses page
│   │   ├── services/          # Services page
│   │   ├── globals.css        # Global styles
│   │   ├── layout.js          # Root layout
│   │   └── page.js            # Home page
│   ├── components/            # Reusable components
│   │   └── Navigation.js      # Main navigation
│   └── sections/              # Page sections
├── public/                    # Static assets
│   ├── robots.txt            # SEO robots file
│   ├── sitemap.xml           # SEO sitemap
│   └── site.webmanifest      # PWA manifest
├── next.config.js             # Next.js configuration
├── tailwind.config.cjs        # Tailwind CSS configuration
├── postcss.config.cjs         # PostCSS configuration
├── vercel.json               # Vercel deployment config
└── package.json               # Dependencies and scripts
```

## 📄 পেজসমূহ

- **হোম**: হিরো সেকশন, ফিচার এবং কল-টু-অ্যাকশন
- **আমাদের সম্পর্কে**: মিশন, ভ্যালু এবং টিম তথ্য
- **সার্ভিস**: শিক্ষামূলক সার্ভিস এবং অফারিংস
- **কোর্স**: অনলাইন কোর্স ক্যাটালগ (মূল্যসহ)
- **যোগাযোগ**: যোগাযোগ ফর্ম এবং তথ্য

## 🚀 কুইক স্টার্ট

### ১. ডিপেন্ডেন্সি ইনস্টল করুন:
```bash
npm install
```

### ২. ডেভেলপমেন্ট সার্ভার চালান:
```bash
npm run dev
```
`http://localhost:3000` এ ভিজিট করুন

### ৩. প্রোডাকশন বিল্ড তৈরি করুন:
```bash
npm run build
```
স্ট্যাটিক ফাইল `out/` ডিরেক্টরিতে জেনারেট হবে।

### ৪. প্রিভিউ দেখুন:
```bash
npm run preview
```

## 🌐 ডিপ্লয়মেন্ট

### Vercel (সুপারিশকৃত)
1. এই রিপোজিটরি GitHub-এ পুশ করুন
2. Vercel-এ প্রজেক্ট ইমপোর্ট করুন
3. Vercel স্বয়ংক্রিয়ভাবে Next.js ডিটেক্ট করে ডিপ্লয় করবে

### অন্যান্য প্ল্যাটফর্ম
- **Netlify**: `out/` ফোল্ডার ড্র্যাগ করে ড্রপ করুন
- **GitHub Pages**: `npm run export` কমান্ড ব্যবহার করুন
- **Firebase**: Firebase CLI ব্যবহার করে ডিপ্লয় করুন

## ⚙️ কনফিগারেশন

### Environment Variables
`.env.local` ফাইল তৈরি করে এনভায়রনমেন্ট ভেরিয়েবল সেট করুন:
```bash
cp .env.example .env.local
```

### SEO এবং PWA
- `public/robots.txt` - সার্চ ইঞ্জিন ক্রলিং কনফিগ
- `public/sitemap.xml` - সাইটম্যাপ ফাইল
- `public/site.webmanifest` - PWA ম্যানিফেস্ট
- `src/app/layout.js` - মেটা ট্যাগ এবং SEO কনফিগ

## 🛠️ টেকনোলজি

- **Next.js 15**: React ফ্রেমওয়ার্ক with App Router
- **Tailwind CSS**: Utility-first CSS ফ্রেমওয়ার্ক
- **DaisyUI**: Tailwind CSS-এর জন্য কম্পোনেন্ট লাইব্রেরি
- **Framer Motion**: অ্যানিমেশন লাইব্রেরি
- **Static Export**: অপটিমাল পারফরমেন্স এবং হোস্টিং ফ্লেক্সিবিলিটি

## 🎨 কাস্টমাইজেশন

- `tailwind.config.cjs`-এ কালার এবং থিম পরিবর্তন করুন
- রেসপেক্টিভ পেজ ফাইলে কনটেন্ট আপডেট করুন
- `src/components/`-এ নতুন কম্পোনেন্ট যোগ করুন
- `src/sections/`-এ সেকশন এক্সটেন্ড করুন

## 📱 রেসপন্সিভ ব্রেকপয়েন্টস

- **xs**: 475px (এক্সট্রা স্মল)
- **sm**: 640px (স্মল)
- **md**: 768px (মিডিয়াম)
- **lg**: 1024px (লার্জ)
- **xl**: 1280px (এক্সট্রা লার্জ)
- **2xl**: 1536px (টু এক্সট্রা লার্জ)

## 🌐 ব্রাউজার সাপোর্ট

- Chrome (লেটেস্ট)
- Firefox (লেটেস্ট)
- Safari (লেটেস্ট)
- Edge (লেটেস্ট)
- Mobile browsers (iOS Safari, Chrome Mobile)

## 📊 পারফরমেন্স

- **Lighthouse Score**: 95+ (পারফরমেন্স, অ্যাক্সেসিবিলিটি, SEO)
- **Static Generation**: দ্রুত লোডিং টাইম
- **Optimized Images**: WebP ফরম্যাট সাপোর্ট
- **Code Splitting**: অটোমেটিক কোড স্প্লিটিং

## 🤝 কন্ট্রিবিউটিং

1. এই রিপোজিটরি ফর্ক করুন
2. ফিচার ব্রাঞ্চ তৈরি করুন (`git checkout -b feature/amazing-feature`)
3. কমিট করুন (`git commit -m 'Add amazing feature'`)
4. পুশ করুন (`git push origin feature/amazing-feature`)
5. Pull Request তৈরি করুন

## 📄 লাইসেন্স

এই প্রজেক্ট MIT লাইসেন্সের অধীন।

---

**তৈরি করেছেন**: Md Abdur Rahman Palash  
**ইনস্টিটিউশন**: আয়মান টিচিং হোম এন্ড আইটি ফার্ম  
**ওয়েবসাইট**: [https://ayman-teaching-home-and-it-firm.vercel.app](https://ayman-teaching-home-and-it-firm.vercel.app)

2. Run development server:
```bash
npm run dev
```
Visit `http://localhost:3001` (or the port shown in terminal)

3. Build for production:
```bash
npm run build
```
Static files will be generated in the `out/` directory.

## Deployment

### Vercel (Recommended)
1. Push this repository to GitHub
2. Import the project on Vercel
3. Vercel will automatically detect Next.js and deploy

### Manual Deployment
The `npm run build` command generates static files in `out/` that can be deployed to any static hosting service.

## Technologies Used

- **Next.js 15**: React framework with App Router
- **Tailwind CSS**: Utility-first CSS framework
- **DaisyUI**: Component library for Tailwind CSS
- **Static Export**: For optimal performance and hosting flexibility

## Customization

- Modify colors and themes in `tailwind.config.cjs`
- Update content in the respective page files
- Add new components in `src/components/`
- Extend sections in `src/sections/`

## Browser Support

- Chrome (latest)
- Firefox (latest)
- Safari (latest)
- Edge (latest)

---

Built for Ayman Teaching & Home IT Firm
